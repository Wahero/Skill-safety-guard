# Skill-safety-guard 優化修改建議方案（P0→P3 分階段）

> **生成日期**：2026-08-25
> **當前版本**：v3.7.0
> **總步驟數**：22 項（P0: 6, P1: 8, P2: 4, P3: 4）
> **已完成**：2 項（p0-1, p0-2）｜**部分完成**：1 項（p0-3）｜**未開始**：19 項
> **提案狀態**：⚠️ 待用戶確認。本文件所列修改在獲得明確確認前，不應對源碼進行任何寫入。

---

## 約束與原則

- **Python 相容**：保持 `requires-python >= 3.8`，所有新增/修改代碼必須在 3.8 上可運行（CI 矩陣含 3.8/3.10/3.11）
- **外部依賴**：僅 `pyyaml>=6.0`，不引入任何新第三方依賴
- **向後相容**：所有 CLI 參數、JSON/SARIF/Markdown 輸出格式、Web API 端點路徑保持不變；唯一新增的是 `--version` 旗標
- **Pro 授權**：維持離線 demo 模式，明確標示「目前為 demo，可被偽造」，不實作非對稱簽名或線上驗證
- **Windows 相容**：保持現有 UTF-8 stdio 重配置、icacls ACL 檢查、路徑 as_posix 正規化
- **倉庫狀態**：不改變公私狀態。所有改動在 Private 和 Public 兩種情況下均適用

---

## 目錄

1. [Phase 0：打包修復與基礎相容性（6 項）](#phase-0打包修復與基礎相容性6-項)
2. [Phase 1：重構與安全加固（8 項）](#phase-1重構與安全加固8-項)
3. [Phase 2：測試與文檔同步（4 項）](#phase-2測試與文檔同步4-項)
4. [Phase 3：擴展同步與漏洞庫改善（4 項）](#phase-3擴展同步與漏洞庫改善4-項)
5. [執行順序與依賴關係](#執行順序與依賴關係)
6. [審批確認區](#審批確認區)

---

## Phase 0：打包修復與基礎相容性（6 項）

### p0-1 修復 Python 3.8 相容性（✅ 已完成）

**涉及檔案**：`src/skill_safety_guard/license.py`

**已完成變更**：

```python
# BEFORE (line 27)
from typing import Optional

# AFTER (line 27)
from typing import Dict, Optional, Tuple
```

```python
# BEFORE (line 182)
def can_scan() -> tuple[bool, dict]:

# AFTER (line 182)
def can_scan() -> Tuple[bool, Dict]:
```

**全專案審計結果**：
- `src/`：僅 `license.py:182` 一處 PEP 585 違規，已修復
- `tests/`、`web/`、`scripts/`：無 PEP 585 違規
- `from __future__ import annotations` 存在於 `web/server.py:20` 和 `web_api.py:7`（安全，無 PEP 585 使用）
- 無 Python 3.10+ union syntax（`|`）使用

**驗證證據**：
- ✅ `ast.parse()` 通過
- ✅ `py_compile` 通過
- ✅ `from skill_safety_guard.license import can_scan` 在 Python 3.11.15 下成功
- ✅ `can_scan()` 返回 `(True, {'tier': 'pro', 'remaining': 'unlimited', ...})`
- 本地無 Python 3.8 環境，但 `Tuple[bool, Dict]` 自 3.5+ 可用

**風險與回退**：無風險。回退即恢復 `tuple[bool, dict]`，但會重新引入 3.8 語法錯誤。

---

### p0-2 補齊 pyproject.toml 依賴宣告（✅ 已完成）

**涉及檔案**：`pyproject.toml`

**已完成變更**：

```toml
# BEFORE (line 10-11)
requires-python = ">=3.8"
license = { text = "MIT" }

# AFTER (line 10-12)
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = ["pyyaml>=6.0"]
```

**驗證證據**：
- ✅ `pip install .` 成功安裝 `skill-safety-guard-3.7.0`
- ✅ `importlib.metadata.requires('skill-safety-guard')` 返回 `['pyyaml>=6.0']`
- ✅ `import yaml` 成功，版本 6.0.3

**風險與回退**：無風險。回退即刪除 `dependencies` 行，但乾淨環境會缺少 pyyaml。

---

### p0-3 修復 rules/ 目錄打包缺失（⚠️ 部分完成，需收尾）

**涉及檔案**：
- `src/skill_safety_guard/rules_loader.py`（已更新 ✅）
- `src/skill_safety_guard/vuln_feed.py`（已更新 ✅）
- `pyproject.toml`（已更新 ✅）
- `scripts/generate_critical_paths_rules.py`（已更新 ✅）
- `scripts/generate_unicode_rules.py`（已更新 ✅）
- `src/skill_safety_guard/reporter.py`（已更新 ✅）
- `src/skill_safety_guard/cli.py`（已更新 ✅）
- `demo/outputs/*.md`（6 份，已更新 ✅）
- `.github/workflows/update-vulns.yml`（已更新 ✅）
- `.github/workflows/ci.yml`（❌ 剩 2 處路徑未修正）
- `package.json`（❌ `"rules"` 待改為 `"src/skill_safety_guard/rules"`，與 p0-6 合併處理）

**已完成變更摘要**：

1. `rules/`（12 檔案）已移至 `src/skill_safety_guard/rules/`
2. `rules_loader.py` `get_rules_dir()` 簡化為單一路徑：

```python
# AFTER
def get_rules_dir() -> Path:
    """獲取規則目錄的絕對路徑（規則隨套件打包，開發/安裝模式統一路徑）"""
    rules_dir = Path(__file__).resolve().parent / "rules"
    if not rules_dir.exists():
        raise FileNotFoundError(f"規則目錄未找到: {rules_dir}")
    return rules_dir
```

3. `vuln_feed.py` 路徑已更新：

```python
# BEFORE (line 36)
BUILTIN_VULNS = Path(__file__).resolve().parent.parent.parent / "rules" / "vulnerabilities.json"
# AFTER
BUILTIN_VULNS = Path(__file__).resolve().parent / "rules" / "vulnerabilities.json"

# BEFORE (line 39)
REMOTE_FEED_URL = "https://raw.githubusercontent.com/Wahero/Skill-safety-guard/main/rules/vulnerabilities.json"
# AFTER
REMOTE_FEED_URL = "https://raw.githubusercontent.com/Wahero/Skill-safety-guard/main/src/skill_safety_guard/rules/vulnerabilities.json"
```

4. `pyproject.toml` package-data 已更新：

```toml
# BEFORE (line 32)
"skill_safety_guard" = []
# AFTER
"skill_safety_guard" = ["rules/*.yaml", "rules/*.json"]
```

**剩餘收尾工作**：

| # | 檔案 | 行號 | 當前 | 修改為 |
|---|------|------|------|--------|
| 1 | `.github/workflows/ci.yml` | 92 | `Path('rules').glob('*.yaml')` | `Path('src/skill_safety_guard/rules').glob('*.yaml')` |
| 2 | `.github/workflows/ci.yml` | 114 | `Path("rules").glob("*.yaml")` | `Path("src/skill_safety_guard/rules").glob("*.yaml")` |
| 3 | `package.json` | 33 | `"rules"` | 移除（已由 `"src"` 涵蓋）；見 p0-6 處理 |

**注意**：ci.yml 第 92、114 行在 `python -c "..."` 內嵌 Python 字串中，需正確處理引號跳脫。

**風險評估**：低。路徑修改為純字串替換。

**回退方案**：反向編輯即可。

**驗證方式**（待執行）：
```bash
pip install .
python -c "from skill_safety_guard.rules_loader import load_all_rules; print(len(load_all_rules()))"
# 預期輸出：10
python tests/test_phase0.py
# V-02/V-04/V-06 全通過
```

---

### p0-4 修復 Web 版號硬編碼（⬜ 未開始）

**涉及檔案**：`web/server.py`

**當前狀況**：
- 第 115 行：`server_version = "skill-safety-guard/0.1"` — 硬編碼為 `0.1`
- 第 167 行：`return self._send_json({"ok": True, "version": "3.6.0", "jobs": len(JOBS)})` — 硬編碼為 `3.6.0`
- 而 `src/skill_safety_guard/__init__.py:2` 定義 `__version__ = "3.7.0"`

**建議修改**：

```python
# web/server.py 頂部（from __future__ import annotations 之後新增）
from skill_safety_guard import __version__

# 第 115 行
# BEFORE
server_version = "skill-safety-guard/0.1"
# AFTER
server_version = f"skill-safety-guard/{__version__}"

# 第 167 行
# BEFORE
return self._send_json({"ok": True, "version": "3.6.0", "jobs": len(JOBS)})
# AFTER
return self._send_json({"ok": True, "version": __version__, "jobs": len(JOBS)})
```

**風險評估**：無風險。僅改字串常數，不影響邏輯。

**回退方案**：還原兩行字串即可。

**驗證方式**：啟動 `python web/server.py` 後 `curl http://localhost:8765/api/health` 應返回 `"version": "3.7.0"`。

---

### p0-5 修復目錄掃描模式漏檢 2 類檢測器（⬜ 未開始）

**涉及檔案**：`src/skill_safety_guard/cli.py`

**當前狀況**：
- 單檔模式（第 172-175 行）：正確使用 **8 個**檢測器（含 `installed_extensions` 和 `prompt_injection`）
- 目錄掃描模式（第 185-187 行）：只使用 **6 個**檢測器，遺漏 `installed_extensions` 和 `prompt_injection`

**建議修改**：

```python
# cli.py 第 184-190 行（BEFORE）
else:
    for det, cat in [(cred_det, "credentials"), (shell_det, "shell"), (path_det, "paths"),
                      (unicode_det, "unicode"), (critical_det, "critical_paths"),
                      (privacy_det, "privacy")]:
        det.category = cat
        result = det.detect_directory(target)
        skill_results[cat] = result

# AFTER — 與單檔模式（第 172-175 行）一致的 8 個檢測器
else:
    for det, cat in [(cred_det, "credentials"), (shell_det, "shell"), (path_det, "paths"),
                      (unicode_det, "unicode"), (critical_det, "critical_paths"),
                      (privacy_det, "privacy"),
                      (installed_ext_det, "installed_extensions"),
                      (prompt_inj_det, "prompt_injection")]:
        det.category = cat
        result = det.detect_directory(target)
        skill_results[cat] = result
```

**注意**：`installed_ext_det` 和 `prompt_inj_det` 變數已在 `scan_target()` 函數中定義（目前為 `CredentialsDetector` 實例，待 p1-3 升級為專屬類別）。目錄掃描使用 `detect_directory()` 方法，需確認這兩個檢測器的 `detect_directory()` 正常運作。

**風險評估**：低。新增檢測器可能產生新發現，但自掃時應被 whitelist 過濾。

**回退方案**：還原迴圈列表即可。

**驗證方式**：
- 掃描 `tests/fixtures/malicious/` 目錄時，`installed_extensions` 和 `prompt_injection` 類別出現在結果中
- 自掃 `python -m skill_safety_guard . --no-pi` 仍為 SAFE / A / 0 發現

---

### p0-6 修復 package.json files 缺 web/（⬜ 未開始）

**涉及檔案**：`package.json`

**當前狀況**（第 29-40 行）：

```json
"files": [
    "SKILL.md",
    "src",
    "scripts",
    "rules",        ← 路徑已不正確（rules/ 已移入 src/ 下）
    "docs",
    "demo",
    "extension",
    "README.md",
    "USAGE.md",
    "LICENSE"
]
```

**建議修改**：

```json
"files": [
    "SKILL.md",
    "src",
    "scripts",
    "docs",
    "demo",
    "web",           ← 新增
    "extension",
    "README.md",
    "USAGE.md",
    "LICENSE"
]
```

**變更說明**：
1. 移除 `"rules"` — rules/ 已移入 `src/skill_safety_guard/rules/`，由 `"src"` 涵蓋
2. 新增 `"web"` — 保證 `pi install` 後 Web 後端（`web/server.py`、`web/index.html`）可用

**風險評估**：低。`web/` 目錄已存在且包含 `server.py` 和 `index.html`。

**回退方案**：還原舊 `files` 陣列即可。

**驗證方式**：`npm pack --dry-run` 輸出應包含 `web/server.py` 和 `web/index.html`，不含獨立 `rules/`。

---

## Phase 1：重構與安全加固（8 項）

### p1-1 拆分 cli.py（命令路由 + 掃描邏輯分離）

**涉及檔案**：
- `src/skill_safety_guard/cli.py`（目前 ~695 行 / 31KB，縮減至 ~180 行）
- 新建 `src/skill_safety_guard/scan_orchestrator.py`（~200 行）
- 新建 `src/skill_safety_guard/commands.py`（~200 行）
- `src/skill_safety_guard/web_api.py`（更新 import）

**當前狀況**：`cli.py` 混合命令路由、掃描邏輯、輸出格式化、誤報處理、安裝決策等全部功能。`web_api.py:19` 從 `cli.py` 導入 `make_install_decision` 和 `scan_target`，並使用 `SimpleNamespace`（第 11 行）模擬 args 物件。

**建議拆分方案**：

| 新檔案 | 職責 | 預估行數 | 從 cli.py 移入的主要函數 |
|--------|------|---------|------------------------|
| `cli.py` | 命令路由 | ~180 | `parse_args()`、`main()` |
| `scan_orchestrator.py` | 掃描編排 | ~200 | `scan_target()`、`resolve_output_file()`、`emit_output()` |
| `commands.py` | 子命令實現 | ~200 | `handle_report_fp()`、`scan_pi_only()`、`format_json_output()`、`make_install_decision()`、`format_decision_block()` |

**關鍵考量**：
- `web_api.py` 改為從 `scan_orchestrator` 導入 `scan_target`
- 建立 `ScanConfig` dataclass 取代 `SimpleNamespace` mock args 的脆弱性
- 不改變任何外部行為：所有 CLI 參數、輸出格式、JSON 結構不變

**風險評估**：中等。重構涉及多檔案 import 鏈調整，需確保無循環導入。

**回退方案**：保留原始 `cli.py` 備份，`git revert` 整個 commit。

**驗證方式**：
- `python -m skill_safety_guard --help` 正常運行
- `python -m skill_safety_guard . --no-pi` 自掃仍為 SAFE
- `python -m skill_safety_guard tests/fixtures/malicious/dangerous_shell` 正常檢出
- `python tests/test_phase0.py` V-02/V-04/V-06 三項全通過

---

### p1-2 統一檔案遍歷（消除 6 次重複 rglob）

**涉及檔案**：
- `src/skill_safety_guard/detectors/base.py`（新增 `detect_directory` 標準實現）
- `src/skill_safety_guard/scan_orchestrator.py`（P1-1 後，實現單次檔案收集）

**當前狀況**：`BaseDetector`（`base.py:54`）只有 `detect_file()` 抽象方法（第 91-93 行），沒有 `detect_directory()` 標準實現。各子檢測器自行遍歷目錄，導致 6-8 個檢測器對同一目錄執行 6-8 次重複 `rglob`。

**建議修改**：

1. `BaseDetector` 新增 `detect_directory()` 方法，接受可選的 `precollected_files` 參數：

```python
def detect_directory(self, directory: Path, precollected: Optional[Dict[Path, str]] = None) -> DetectionResult:
    """檢測整個目錄。若 precollected 為 None，自行遍歷。"""
    files = precollected if precollected is not None else self._collect_files(directory)
    result = DetectionResult(category=self.category, scanned_files=len(files))
    for path, content in files.items():
        try:
            findings = self.detect_file(path, content)
            for f in findings:
                if not self._apply_whitelist(f):
                    f = self._apply_confidence_demotion(f)
                    result.findings.append(f)
        except Exception:
            pass
    return result
```

2. `scan_orchestrator.py` 中只呼叫一次 `_collect_all_files()`，然後分發給所有檢測器：

```python
def _collect_all_files(directory: Path) -> Dict[Path, str]:
    """單次遍歷收集所有文字檔案"""
    files = {}
    for f in directory.rglob("*"):
        if f.is_file() and _is_text_file(f):
            try:
                files[f] = f.read_text(encoding="utf-8")
            except (UnicodeDecodeError, PermissionError):
                pass
    return files
```

**風險評估**：低。純性能優化，不改變檢測結果。需注意大型倉庫的記憶體佔用。

**回退方案**：保留原有的各子類遍歷邏輯作為 fallback。

**驗證方式**：掃描結果與修改前完全一致；大目錄掃描時間不增加。

---

### p1-3 為 installed_extensions 和 prompt_injection 建立專屬檢測器

**涉及檔案**：
- 新建 `src/skill_safety_guard/detectors/installed_extensions.py`
- 新建 `src/skill_safety_guard/detectors/prompt_injection.py`
- `src/skill_safety_guard/detectors/__init__.py`（更新匯出）
- `src/skill_safety_guard/cli.py`（更新 import 和檢測器初始化）
- `src/skill_safety_guard/web_api.py`（更新 import）

**當前狀況**：`cli.py` 中 `installed_ext_det` 和 `prompt_inj_det` 實際上都是 `CredentialsDetector` 實例（巧合複用：YAML 規則結構與憑證檢測相似）。`detectors/__init__.py` 目前只匯出 6 個檢測器類別。

**建議修改**：

```python
# detectors/installed_extensions.py
class InstalledExtensionsDetector(BaseDetector):
    """已安裝擴展檢測器"""
    category: str = "installed_extensions"

    def detect_file(self, file_path: Path, content: str) -> List[Finding]:
        findings = []
        for rule, compiled in self._iter_compiled_rules():
            for m in compiled.finditer(content):
                findings.append(Finding(
                    rule_id=rule["id"],
                    rule_name=rule["name"],
                    severity=rule.get("severity", "medium"),
                    confidence=rule.get("confidence", "medium"),
                    category=self.category,
                    description=rule.get("description", ""),
                    remediation=rule.get("remediation", ""),
                    file_path=str(file_path),
                    line_number=content[:m.start()].count("\n") + 1,
                    matched_text=m.group(0)[:200],
                ))
        return findings

# detectors/prompt_injection.py — 同理，category = "prompt_injection"
```

`detectors/__init__.py` 新增匯出：

```python
from .installed_extensions import InstalledExtensionsDetector
from .prompt_injection import PromptInjectionDetector

__all__ = [
    ...,  # 現有 6 個
    "InstalledExtensionsDetector",
    "PromptInjectionDetector",
]
```

**風險評估**：低。現有 YAML 規則結構不變，新檢測器行為與現有 `CredentialsDetector` 複用一致。

**回退方案**：回退到 `CredentialsDetector` 複用模式。

**驗證方式**：`test_phase0.py` 的 V-04 惡意樣本檢出率仍為 8/8。

---

### p1-4 新增 --version CLI 選項

**涉及檔案**：`src/skill_safety_guard/cli.py`

**建議修改**：在 `parse_args()` 中（`return parser.parse_args(argv)` 之前）新增：

```python
parser.add_argument(
    "--version",
    action="version",
    version=f"skill-safety-guard v{__version__}",
    help="顯示版本號",
)
```

需在檔案頂部確保 `from . import __version__` 或 `from skill_safety_guard import __version__`。

**風險評估**：無風險。`argparse` 原生支援 `action="version"`，行為為列印後 `sys.exit(0)`。

**回退方案**：移除該行即可。

**驗證方式**：`safety-check --version` 輸出 `skill-safety-guard v3.7.0`；`--help` 中顯示 `--version` 選項。

---

### p1-5 git clone 沙箱加固

**涉及檔案**：`src/skill_safety_guard/scan_target_resolver.py`

**當前狀況**（第 118-124 行）：

```python
cmd = [
    "git", "clone",
    "--depth=1",
    f"https://github.com/{user}/{repo}.git",
    str(tmp_dir),
]
```

未禁用 hooks，惡意倉庫可透過 `.git/hooks` 在 clone 時執行任意程式碼。

**建議修改**：

```python
import platform

hooks_path = "NUL" if platform.system() == "Windows" else "/dev/null"
cmd = [
    "git", "clone",
    "--depth=1",
    "-c", f"core.hooksPath={hooks_path}",     # 禁用 hooks
    "-c", "protocol.file.allow=never",        # 禁用 file:// 協議
    f"https://github.com/{user}/{repo}.git",
    str(tmp_dir),
]
```

**風險評估**：極低。`core.hooksPath` 設定為 null 設備僅影響此次 clone。

**回退方案**：移除兩行 `-c` 參數即可。

**驗證方式**：掃描含惡意 hooks 的測試倉庫時 hooks 不被執行；正常 GitHub URL 掃描不受影響。

---

### p1-6 Web 安全加固（CORS 收窄 + rate limit + demo 標記）

**涉及檔案**：`web/server.py`

**當前狀況**：
- 第 123 行：`self.send_header("Access-Control-Allow-Origin", "*")` — 允許任意來源
- 無 rate limiting

**建議修改**：

1. **CORS 收窄**：

```python
# _send_json 方法中
ALLOWED_ORIGINS = {"http://localhost:8765", "http://127.0.0.1:8765",
                   "http://localhost:3000", "http://127.0.0.1:3000"}
origin = self.headers.get("Origin", "")
if origin in ALLOWED_ORIGINS:
    self.send_header("Access-Control-Allow-Origin", origin)
```

2. **Rate limiting**（記憶體版，每 IP 每分鐘最多 5 次 POST）：

```python
from collections import defaultdict
import time

_rate_limits: Dict[str, list] = {}

def _check_rate_limit(self) -> bool:
    ip = self.client_address[0]
    now = time.time()
    window = _rate_limits.setdefault(ip, [])
    window[:] = [t for t in window if now - t < 60]
    if len(window) >= 5:
        return False
    window.append(now)
    return True
```

在 `do_POST()` 中呼叫，超額返回 429。

3. **Demo 標記**：Web 啟動時 banner 顯示版本號和 `⚠️ 目前為 demo 模式`。

**風險評估**：低。CORS 收窄僅影響瀏覽器跨域請求；rate limit 僅影響高頻請求。

**回退方案**：還原 CORS 為 `*`，移除 rate limit 檢查。

**驗證方式**：
- `curl -H "Origin: http://evil.com" http://localhost:8765/api/health` → CORS 頭不含 `evil.com`
- 連續 6 次 `POST /api/scans` → 第 6 次回傳 429

---

### p1-7 license.py 明確標示 demo 層級

**涉及檔案**：`src/skill_safety_guard/license.py`

**當前狀況**：模組 docstring（第 1-18 行）提到「演示用 SECRET_KEY」但不夠醒目。`generate_license_key()`（第 65-87 行）和 `print_tier_banner()` 未標示 demo。

**建議修改**：

1. **模組 docstring** 加入醒目警告：

```python
"""⚠️ DEMO 層級授權系統

本授權系統為 demo 層級。SECRET_KEY 隨原始碼公開，任何人可偽造 Pro key。
正式部署應使用環境變數 SSG_LICENSE_SECRET 或非對稱簽名（如 Ed25519 公鑰驗證）。
"""
```

2. **`print_tier_banner()`**：Pro 用戶顯示 `[PRO (demo)]` 而非 `[PRO]`。

3. **`generate_license_key()`**（第 87 行後）：輸出中加入警告：

```python
print("⚠️ 此密鑰僅用於測試/demo，正式部署不可用。")
return key
```

**風險評估**：無風險。純文字標記修改，不影響邏輯。

**回退方案**：刪除新增的文字即可。

**驗證方式**：`safety-check --license-status` 和 `--generate-pro-key` 輸出含 demo 標記。

---

### p1-8 llm_check.py 隱私加固提示

**涉及檔案**：`src/skill_safety_guard/llm_check.py`

**當前狀況**：`_read_pi_auth_key()`（第 28-46 行）直接讀取 `~/.pi/agent/auth.json` 並將 API key 傳送給 DeepSeek。`get_api_key()`（第 58-63 行）的優先序為 `DEEPSEEK_API_KEY` → `OPENAI_API_KEY` → `_read_pi_auth_key()`，但讀取 auth.json 時無任何警告。

**建議修改**：

1. `get_api_key()` 中，當從 `auth.json` 讀取 key 時（非環境變數），印出 stderr 警告：

```python
def get_api_key() -> Optional[str]:
    env_key = os.environ.get("DEEPSEEK_API_KEY") or os.environ.get("OPENAI_API_KEY")
    if env_key:
        return env_key
    auth_key = _read_pi_auth_key()
    if auth_key:
        import sys
        print(
            "[隱私提示] 使用來自 ~/.pi/agent/auth.json 的 API key。"
            "建議改用環境變數 DEEPSEEK_API_KEY 以避免意外洩漏。",
            file=sys.stderr
        )
    return auth_key
```

2. 更新模組 docstring 記錄此行為。

**風險評估**：極低。僅新增 stderr 提示，不改變實際行為。

**回退方案**：刪除新增的警告行。

**驗證方式**：`--pro` 模式下輸出含隱私警告（若使用 auth.json 而非環境變數）。

---

## Phase 2：測試與文檔同步（4 項）

### p2-1 標準化測試架構與補測試用例

**涉及檔案**：
- 新建 `tests/test_detectors.py`
- 新建 `tests/test_cli.py`
- 新建 `tests/test_scan_target_resolver.py`
- 新建 `tests/test_license.py`
- 保留 `tests/test_phase0.py`（端到端驗證）
- `.github/workflows/ci.yml`（新增 pytest 步驟）

**當前狀況**：僅 `tests/test_phase0.py` 存在（V-02/V-04/V-06 端到端驗證），無標準 pytest 單元測試。`tests/fixtures/` 下有 `clean/` 和 `malicious/` 樣本目錄。

**建議新增測試**：

| 測試檔案 | 測試內容 | 預估用例數 |
|---------|---------|-----------|
| `test_detectors.py` | 6 個檢測器各自的 `detect_file` 單元測試（正樣本 + 負樣本） | 12 |
| `test_cli.py` | `parse_args` 各參數組合、`--version`、`--report-fp`、子命令派發 | 5 |
| `test_scan_target_resolver.py` | GitHub URL 解析（4 種格式）、本地路徑、paste 模式 | 6 |
| `test_license.py` | `can_scan`/`record_scan`/週重置邏輯/`verify_license_key` | 4 |

**CI 更新**（`ci.yml` 新增步驟）：

```yaml
- name: Run unit tests
  env:
    PYTHONIOENCODING: utf-8
  run: |
    python -m pytest tests/ -q --tb=short
```

**風險評估**：無風險。純新增測試，不涉及生產代碼修改。

**回退方案**：刪除新增測試檔案即可。

**驗證方式**：`python -m pytest tests/ -v` 至少 15 個用例通過。

---

### p2-2 補 V-03（Pi 版本檢測）和 V-05（安裝前攔截）自動化測試

**涉及檔案**：
- 新建 `tests/test_pi_version.py`（V-03）
- 新建 `tests/test_extension_rules.py`（V-05，或 Python 端驗證）
- `src/skill_safety_guard/pi_check/version.py`（被測）
- `extension/safety-guard.ts`（被測，V-05）

**V-03 測試方案**（`test_pi_version.py`）：

```python
def test_v03_pi_version_cve():
    """V-03: Pi 版本 CVE 檢測"""
    from skill_safety_guard.pi_check.version import _parse_version, _is_below

    assert _parse_version("0.81.0") == (0, 81, 0)
    assert _is_below((0, 81, 0), (0, 84, 0))  # 0.81.0 < 0.84.0
    # CVE-2026-54326 影響 < 0.84.0 → 0.81.0 命中
    # CVE-2026-54327 影響 < 0.86.0 → 0.84.0 命中
    # 0.86.0 不命中任何 CVE
```

**V-05 測試方案**（`test_extension_rules.py`）：Python 端驗證 TS 正則一致性（從 YAML 讀取規則，比對 TS 原始碼中的 RegExp 字串）。

**風險評估**：無風險。純測試新增。

**回退方案**：刪除測試檔案。

**驗證方式**：`python -m pytest tests/test_pi_version.py tests/test_extension_rules.py -v` 通過。

---

### p2-3 同步文檔版本與清理根目錄

**涉及檔案**：
- `功能說明書.md`（更新至 v3.7.0）
- `PROGRESS.md`（標記為歸檔）
- `TASKS.md`（更新至 v3.7.0 狀態）
- 根目錄清理：`sudoku6x6.html`、`sudoku6x6(DS4F).html`、`frontend-design-function.md`、`打賞方案.md` → 移至 `archive/`
- `.gitignore`（確認已涵蓋 `scan-report-*.md` 和 `report/`）

**當前狀況**：
- 根目錄有 4 份不相關檔案（sudoku html、設計稿、打賞方案）
- 文檔版本可能未同步至 3.7.0

**文檔一致性檢查點**：

| 文檔 | 規則數 | 版本號 | 類別數 |
|------|--------|--------|--------|
| README.md | 187 | 3.7.0 | 10 |
| SKILL.md | 187 | 3.7.0 | 10 |
| 功能說明書.md | 187 | 3.7.0 | 10 |

**風險評估**：極低。純文檔和檔案組織變更。

**回退方案**：從 `archive/` 移回根目錄。

**驗證方式**：三處文檔規則數和版本號一致。

---

### p2-4 報告目錄自動清理策略

**涉及檔案**：
- `src/skill_safety_guard/web_api.py`（新增清理函數）
- `web/server.py`（啟動時執行清理）

**當前狀況**：`web_api.py` 寫入報告到 `report/` 目錄，無清理機制。

**建議修改**：

```python
# web_api.py 新增
def _cleanup_reports(max_keep: int = 50) -> int:
    """清理 report/ 目錄，保留最近 max_keep 份"""
    report_dir = Path.cwd() / "report"
    if not report_dir.exists():
        return 0
    files = sorted(report_dir.glob("*.md"), key=lambda f: f.stat().st_mtime, reverse=True)
    removed = 0
    for f in files[max_keep:]:
        f.unlink()
        removed += 1
    return removed
```

在 `web/server.py` 啟動時呼叫 `_cleanup_reports()` 一次。

**風險評估**：極低。僅刪除舊報告檔案。

**回退方案**：移除清理調用。

**驗證方式**：手動產生 55 份報告後，`report/` 僅保留 50 份。

---

## Phase 3：擴展同步與漏洞庫改善（4 項）

### p3-1 TS 擴展規則與 YAML 同步（generator 腳本）

**涉及檔案**：
- 新建 `scripts/generate_extension_rules.py`（generator 腳本）
- 新建 `extension/safety-guard-rules.ts`（generator 產出）
- `extension/safety-guard.ts`（修改為從 data 檔匯入）
- `package.json`（新增 `scripts.generate-rules`）
- `.github/workflows/ci.yml`（新增同步檢查步驟）

**當前狀況**：`extension/safety-guard.ts` 第 26-51 行硬編碼 `SHELL_RULES`（12 條），第 54-65 行硬編碼 `CRED_RULES`（部分）。與 YAML 規則手動同步，容易不同步。

**Generator 腳本設計**（`scripts/generate_extension_rules.py`）：

```python
"""從 YAML 規則生成 TypeScript 規則資料檔"""
import yaml
from pathlib import Path

RULES_DIR = Path(__file__).resolve().parent.parent / "src" / "skill_safety_guard" / "rules"
OUTPUT = Path(__file__).resolve().parent.parent / "extension" / "safety-guard-rules.ts"

def generate():
    for source, var_name in [("dangerous_shell.yaml", "SHELL_RULES"),
                              ("credentials.yaml", "CRED_RULES")]:
        with open(RULES_DIR / source, encoding="utf-8") as f:
            data = yaml.safe_load(f)
        rules = data.get("rules", [])
        # 生成 TS 陣列匯出
        ...
```

**safety-guard.ts 修改**：

```typescript
// 移除硬編碼的 SHELL_RULES / CRED_RULES
import { SHELL_RULES, CRED_RULES } from "./safety-guard-rules";
```

**CI 檢查**：

```yaml
- name: Check extension rules sync
  run: |
    python scripts/generate_extension_rules.py
    git diff --exit-code extension/safety-guard-rules.ts
```

**風險評估**：中等。需確保 TS 生成正確且無語法錯誤。

**回退方案**：還原 `safety-guard.ts` 為硬編碼版本。

**驗證方式**：generator 產出規則數與 YAML 一致（12 shell + 8 cred）；CI 檢查通過。

---

### p3-2 拆分 vuln_feed.py 並去重 CI 邏輯

**涉及檔案**：
- `src/skill_safety_guard/vuln_feed.py`（目前 ~797 行 / 29KB，縮減至 ~250 行）
- 新建 `src/skill_safety_guard/vuln_feed_sources.py`（~250 行）
- 新建 `src/skill_safety_guard/vuln_feed_config.py`（~150 行）
- `.github/workflows/update-vulns.yml`（簡化）

**當前狀況**：`vuln_feed.py` 單文件包含核心載入/快取/更新編排/OSV 查詢/配置管理。`update-vulns.yml` 中內嵌 ~100 行 Python OSV 抓取代碼（第 27-131 行），與 `vuln_feed.py` 部分重複。

**建議拆分**：

| 新檔案 | 職責 | 預估行數 | 主要內容 |
|--------|------|---------|---------|
| `vuln_feed.py` | 核心載入/快取/更新編排/版本檢查 | ~250 | `check_and_update_vulns()`、`update_vulnerabilities()`、`get_vuln_source_info()` |
| `vuln_feed_sources.py` | OSV.dev 查詢 + 解析 | ~250 | `_query_osv_package()`、`_query_osv_batch()`、`_parse_osv_vuln()`、`_get_proxy_urls()` |
| `vuln_feed_config.py` | 配置管理 | ~150 | `FREQUENCY_TTL`、`GITHUB_PROXIES`、`DOMESTIC_SOURCES`、`TRACKED_PACKAGES`、配置讀寫 |

**update-vulns.yml 簡化**：內嵌的 ~100 行 Python 改為：

```yaml
- name: Update vulnerabilities
  run: |
    pip install .
    python -c "from skill_safety_guard.vuln_feed import update_vulnerabilities; print(update_vulnerabilities(force=True))"
```

**風險評估**：中等。涉及 import 鏈調整，需確保無循環導入。

**回退方案**：保留原始單文件版本，`git revert` 整個 commit。

**驗證方式**：
- `safety-check --update-vulns` 功能不變
- `safety-check --vuln-status` 正常顯示
- CI workflow 正常執行並 commit

---

### p3-3 移除未實作的 NVD 鏡像標記

**涉及檔案**：`src/skill_safety_guard/vuln_feed.py`

**當前狀況**：
1. `DOMESTIC_SOURCES`（第 78 行附近）中將 `nvd_github_mirror` 標記為 `auto_usable=True`
2. 第 273-286 行 `_query_github_nvd_mirror()` 方法始終 `return None`（未實作，第 286 行 `return None`）

**建議修改**：

1. 移除 `_query_github_nvd_mirror()` 函數（第 273-286 行）
2. `DOMESTIC_SOURCES["nvd_github_mirror"]` 中：

```python
# BEFORE
"auto_usable": True,
# AFTER
"auto_usable": False,
"access": "鏡像存在但 CPE 匹配太複雜，暫未實作自動查詢",
```

3. 移除 `_osv_query` 中對該方法的 fallback 呼叫（若有）

**風險評估**：極低。該方法從未實際執行過（始終 `return None`）。

**回退方案**：保留該方法。

**驗證方式**：`safety-check --vuln-sources` 輸出不再標記 `nvd_github_mirror` 為自動可用；漏洞檢測功能不受影響。

---

### p3-4 vuln_feed 漏洞庫原子寫入

**涉及檔案**：`src/skill_safety_guard/vuln_feed.py`

**當前狀況**：`update_vulnerabilities()` 寫入 `LOCAL_VULNS_CACHE`（`CACHE_DIR / "vulnerabilities_cache.json"`）和 `_write_update_meta()` 寫入 `UPDATE_META` 時直接覆蓋，進程中斷可能留下損壞的 JSON。

**建議修改**：

```python
def _atomic_write_json(data, path: Path):
    """原子寫入 JSON 檔案"""
    tmp = path.with_suffix(".json.tmp")
    tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(path)  # 原子替換（Windows 上 os.replace 等價）
```

所有寫入 `LOCAL_VULNS_CACHE` 和 `UPDATE_META` 的位置改為使用 `_atomic_write_json()`。

**風險評估**：無風險。純寫入安全性改善。

**回退方案**：回復到直接寫入。

**驗證方式**：手動中斷更新過程後，`LOCAL_VULNS_CACHE` 仍為完整 JSON 或不存在（不會有半寫檔案）。

---

## 執行順序與依賴關係

```
proposal-doc (本文件) → 等待用戶確認 →
  P0-1 (已完成) → P0-2 (已完成) → P0-3 (收尾) → P0-4 → P0-5 → P0-6
    → P1-1 (依賴 P0-3 打包驗證) → P1-2 (依賴 P1-1) → P1-3 (依賴 P1-1)
      → P1-4 → P1-5 → P1-6 → P1-7 → P1-8
        → P2-1 (依賴 P1-1~P1-4) → P2-2 (依賴 P1-5) → P2-3 → P2-4
          → P3-1 → P3-2 → P3-3 → P3-4
```

Phase 0 是 Phase 1 的前置（打包修復後才能 `pip install` 測試）。Phase 1 是 Phase 2 的前置（cli 拆分後才容易加測試）。Phase 3 與 Phase 2 可並行。

---

## 審批確認區

- [ ] 確認執行全部 22 項（p0-1 至 p3-4）
- [ ] 確認執行部分項目（請指定）
- [ ] 要求修改方案（請說明）

> 請回覆「確認執行」或指出需要調整的項目。確認後我將按順序實施，每步驟完成後記錄 checkpoint。

---

*本文件由 skill-safety-guard 優化計畫自動生成，基於截至 2026-08-25 的實際檔案檢查結果。*
