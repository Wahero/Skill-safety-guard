# skill-safety-guard 功能說明書

> **完整功能規格**（截至 2026-08-26，功能線 v3.7.0 / 187 條規則 / 10 類檢測）
> 定位：個人開發者安裝 Skill/MCP 前的安全守護者

---

## 目錄

1. [專案定位](#1-專案定位)
2. [系統架構](#2-系統架構)
3. [檢測能力總覽](#3-檢測能力總覽)
4. [功能模組明細](#4-功能模組明細)
5. [CLI 命令參考](#5-cli-命令參考)
6. [輸出格式](#6-輸出格式)
7. [漏洞情報系統](#7-漏洞情報系統)
8. [Freemium 許可系統](#8-freemium-許可系統)
9. [誤報治理](#9-誤報治理)
10. [測試與 CI](#10-測試與-ci)
11. [版本歷史](#11-版本歷史)
12. [安裝方式](#12-安裝方式)
13. [已知限制](#13-已知限制)

---

## 1. 專案定位

| 維度 | 內容 |
|------|------|
| **形態** | Pi Agent 對話式 Skill（同時可獨立 CLI 使用） |
| **目標用戶** | 個人開發者（非企業安全團隊） |
| **核心場景** | 安裝第三方 Skill/MCP **之前**的安全檢查 |
| **核心理念** | 殺手場景 > 功能數；誤報率 > 檢出率 |
| **商業模式** | Freemium（免費 5 次/週，Pro $4.99/月） |
| **差異化** | 規則庫開源、對話式觸發、誤報反饋渠道、置信度分級 |
| **競品對照** | 避開 NVIDIA SkillSpector（企業）正面競爭，只做個人開發者 Skill 層 |

**真實威脅背景**（2026-01 ClawHavoc 攻擊）：1,184 個惡意 Skill、230 萬美元被盜。傳統安全工具對 Skill 攻擊（自然語言指令載體）檢出率為 0.00%。

---

## 2. 系統架構

```
用戶輸入 /safety-check <target>
   ↓
Pi Agent 加載 SKILL.md → 調用 CLI
   ↓
python -m skill_safety_guard <target>
   ↓
三層掃描：
  ① Pi Agent 全局（版本 CVE + auth.json ACL 權限）
  ② Skill 內容（9 類規則：181 條）
  ③ Skill 依賴（MCP 靜態分析，--all 時）
   ↓
Markdown / JSON / SARIF 報告（風險等級 + 安裝建議 + 置信度標記）
```

### 代碼結構

```
src/skill_safety_guard/
├── cli.py                    # CLI 入口 + 掃描流程編排
├── __main__.py               # python -m 入口（UTF-8 stdio 重配置）
├── detectors/
│   ├── base.py               # BaseDetector（文件收集/白名單/置信度）
│   ├── credentials.py        # 憑證洩露檢測器
│   ├── shell.py              # 危險 Shell 檢測器
│   ├── paths.py              # 敏感路徑檢測器
│   ├── unicode.py            # Unicode 隱寫檢測器
│   └── critical_paths.py     # 關鍵系統參數修改檢測器
├── mcp_check.py              # MCP 依賴檢查（F-029~F-032）
├── llm_check.py              # LLM 輔助提示詞注入檢測（--pro）
├── pi_check/                 # Pi 全局檢查（版本 CVE + auth.json ACL）
├── rules_loader.py           # YAML 規則載入
├── scan_target_resolver.py   # 目標解析（本地/GitHub URL/子目錄/paste）
├── reporter.py               # Markdown 報告生成（含掃描結論三部分）
├── sarif.py                  # SARIF 輸出
├── license.py                # Freemium 許可證
└── vuln_feed.py              # 漏洞情報系統
```

### 掃描流程細節

| 步驟 | 說明 |
|------|------|
| 目標解析 | 本地路徑 / GitHub URL（4 種格式）/ 子目錄 / 單文件 / paste |
| 文件收集 | 遞迴掃描文本文件；跳過 .git、node_modules、構建產物、測試 fixtures（僅整倉庫掃描時） |
| 規則匹配 | 正則預編譯緩存 + 逐行掃描 |
| 白名單 | whitelisted_patterns（rule_id+pattern，支援 match_context）+ whitelisted_paths（Windows 路徑正規化） |
| 置信度 | 🔴 高 / 🟡 中 / 🟢 低；可降級 |
| 評級 | 綜合風險等級 A-F + 三級決策（SAFE/CAUTION/DANGER） |

---

## 3. 檢測能力總覽

**181 條規則，9 個檢測類別**（實測規則數）：

| 類別 | 規則數 | 檢測內容 |
|------|--------|---------|
| 🚨 關鍵系統參數修改（critical_paths） | 67 | AI Agent 配置 / Shell init / 包管理器 / 持久化 / 編輯器 / Git / 歷史篡改 / Rootkit / 數據庫 |
| 💉 提示詞注入（prompt_injection） | 14 | Ignore previous instructions / 系統提示提取 / 越獄 / 角色劫持 / 數據外洩 |
| 🔌 MCP 基礎（mcp） | 17 | npx -y 未知名包 / curl\|bash 伺服器 / 工具名暗示 / HTTP 明文 |
| 💥 MCP 注入模式（mcp_injection） | 22 | SQL 注入（7）/ 命令注入（7）/ 路徑遍歷（5）/ 邏輯注入（3） |
| 🔑 憑證洩露（credentials） | 10 | OpenAI / Anthropic / AWS / GitHub / Slack / Stripe / Google / JWT / PEM |
| 💀 危險 Shell（dangerous_shell） | 12 | curl\|bash / 反向 Shell / rm -rf / dd / fork bomb / base64 混淆 |
| 📁 敏感路徑（sensitive_paths） | 14 | ~/.ssh / ~/.aws / .env / Docker / K8s / /etc/passwd / gnupg |
| 🕵️ Unicode 隱寫（unicode_steganography） | 14 | 零寬字符 / Tag 區塊 / 不可見運算符 / 混合隱寫 |
| 🔌 已安裝擴展審計（installed_extensions） | 11 | eval/exec / 寫主目錄 / 危險 shell |

### 關鍵系統參數修改（67 條，最高優先級）

| 覆蓋範圍 | 明細 |
|---------|------|
| **AI Agent 配置** | Pi / Claude / Cursor / Codex / Continue / Aider / OpenCode / Cline / Cody |
| **Shell init** | ~/.bashrc、~/.zshrc、~/.profile（寫入/重定向/刪除） |
| **包管理器** | npm / Yarn / pip / Cargo / gem / Composer / Maven / Gradle / Bower |
| **持久化** | macOS LaunchAgents/Daemons、Linux autostart、Systemd user services |
| **編輯器** | VSCode / Vim / Emacs / Neovim / nano |
| **Git** | .gitconfig（core.sshCommand / hooksPath / credential.helper / include.path） |
| **歷史篡改** | bash/zsh/python_history、history -c && rm |
| **Rootkit** | /etc/ld.so.preload、/etc/hosts、/etc/resolv.conf、/etc/environment |
| **數據庫** | .pgpass、.my.cnf、.mongoshrc.js |

### 第一層：Pi Agent 全局

| 檢測項 | 說明 |
|--------|------|
| Pi 版本 CVE | 檢查版本是否在 CVE-2026-54326/54327 受影響範圍 |
| auth.json 權限 | Windows ACL（icacls）/ Linux POSIX（os.stat） |

---

## 4. 功能模組明細

### 4.1 掃描目標（殺手場景）

| 目標類型 | 示例 | 說明 |
|---------|------|------|
| 本地目錄 | `./my-skill` | 遞迴掃描所有文本文件 |
| 本地文件 | `./SKILL.md` | 單文件掃描 |
| GitHub 倉庫 | `https://github.com/user/repo` | 克隆後掃描（預覽式，不安裝不執行） |
| GitHub 子目錄 | `.../tree/main/skills/foo` | 只掃子目錄 |
| GitHub 單文件 | `.../blob/main/SKILL.md` | 只掃文件 |
| raw 文件 | `https://raw.githubusercontent.com/...` | 直接下載掃描 |
| 粘貼內容 | `paste` | 從 stdin 讀取 |
| 當前目錄 | `.`（默認） | 無參數時 |

**三級決策**：🟢 SAFE（可安裝）/ 🟡 CAUTION（人工複查）/ 🔴 DANGER（不要安裝）

### 4.2 掃描模式

| 模式 | 命令 | 內容 |
|------|------|------|
| 默認 | `safety-check <target>` | Skill 內容掃描 |
| Pi 全局 | `--pi` | 只掃 Pi Agent 全局 |
| 完整 | `--all` | Pi + Skill + MCP 依賴一次完成 |
| 快速 | `--no-pi` | 跳過 Pi 檢查（加速） |
| Pro | `--pro` | 啟用 LLM 輔助檢測 |
| 擴展審計 | `--audit-extensions` | 掃描已安裝擴展目錄 |

### 4.3 報告自動生成

- GitHub URL 掃描 → 自動生成 `scan-report-<repo>.md` 到當前目錄
- `--output-file <path>` 可覆蓋路徑
- 本地掃描不自動寫檔（需顯式 `--output-file`）

### 4.4 掃描結論（報告尾段，統一三部分）

1. **文字結論**：風險等級 + 安裝建議（可安裝/複查/拒裝）
2. **結論說明**：統計 + 主要風險源分析 + 誤報說明
3. **漏洞數字卡片**：嚴重度統計表（含誤報排除行）

### 4.5 誤報自動識別

- `path-env-file` 命中 `os.environ.get()` / `os.getenv()` 環境變數讀取模式 → 自動判定誤報（憑證不寫入原始碼是安全做法）
- 誤報項標示 ✅ 誤報判定，統計與風險等級自動排除

---

## 5. CLI 命令參考

```bash
# 掃描
safety-check                          # 掃描當前目錄
safety-check <target>                 # 掃描目標（路徑/URL/paste）
safety-check --pi                     # 只檢查 Pi 全局
safety-check --all                    # 完整掃描（Pi + Skill + MCP）
safety-check --no-pi                  # 跳過 Pi 檢查

# 輸出
--output {markdown,json,sarif}        # 輸出格式
--output-file PATH                    # 報告寫入指定檔案
--no-color                            # 禁用彩色
--min-confidence {high,medium,low}    # 置信度過濾
--confidence-detail                   # 顯示置信度原因

# 漏洞情報
--update-vulns                        # 更新漏洞庫
--osv                                 # OSV.dev 實時查詢
--vuln-status                         # 漏洞庫狀態
--vuln-sources                        # 所有漏洞源
--vuln-frequency {daily,weekly,monthly,off}
--vuln-proxy URL                      # GitHub 加速代理

# 許可
--activate-pro KEY                    # 激活 Pro
--generate-pro-key                    # 生成測試金鑰
--license-status                      # 許可狀態
--pro                                 # 啟用 LLM 檢測

# 其他
--audit-extensions                    # 已安裝擴展審計
--report-fp <rule-id>                 # 報告誤報
--help                                # 幫助
```

---

## 6. 輸出格式

| 格式 | 用途 | 特點 |
|------|------|------|
| **Markdown**（默認） | 人類閱讀 | 風險等級 A-F、三級決策、置信度標記、掃描結論三部分 |
| **JSON** | 機器可讀 / CI | 結構化 findings |
| **SARIF** | GitHub Code Scanning | 可在 GitHub Security 標籤頁查看 |

---

## 7. 漏洞情報系統

### 架構（三層）

```
Layer 1: 內置基線 rules/vulnerabilities.json（隨倉庫發布，離線可用）
Layer 2: 權威源自動更新（GitHub Actions 每天 00:00 UTC 拉取 OSV.dev）
Layer 3: OSV.dev 實時查詢（--osv，零日覆蓋）
```

### 數據源

| 來源 | 權威性 | 自動可用 | 說明 |
|------|--------|---------|------|
| **OSV.dev**（主） | ⭐⭐⭐⭐⭐ Google 官方 | ✅ | 自動排除已撤銷 CVE；查詢必須帶 ecosystem 字段 |
| **AVID**（後備） | ⭐⭐⭐⭐ 開源 | ✅ | GitHub API 拉取，OSV 失敗時自動回退 |
| **CAIVD**（國內） | ⭐⭐⭐⭐⭐ 工信部/信通院 | ❌ 免註冊人工查詢 | 數據前端渲染，無公開 API |
| **CNNVD/CNVD**（國內） | ⭐⭐⭐⭐⭐ | ❌ 需註冊 | 反爬限制 |
| **本倉庫漏洞庫** | ⭐⭐⭐⭐ 社區+自動 | ✅ | GitHub Actions 每天更新 |
| ~~NVD~~ | ❌ 已 EOL | — | API 2.0 已停服 |

### 自動更新機制

- 每次掃描檢查 TTL（默認 weekly，可配置），過期後台自動更新（不阻塞）
- 多源回退鏈：OSV → AVID → 本倉庫庫 → 加速代理（ghproxy）
- 國內用戶：`--vuln-proxy https://ghproxy.net/`

---

## 8. Freemium 許可系統

| 層級 | 價格 | 內容 |
|------|------|------|
| **免費** | $0 | 每週 5 次掃描，所有基礎檢測功能 |
| **Pro** | $4.99/月 | 無限掃描 + LLM 輔助提示詞注入檢測 |

```bash
# 測試 Pro（本地生成，僅測試用）
safety-check --generate-pro-key
safety-check --activate-pro ssg-pro-XXXX-...
safety-check --license-status
```

---

## 9. 誤報治理

> **原則**：誤報率 > 檢出率。一個誤報會讓用戶卸載插件。

### 機制

| 機制 | 說明 |
|------|------|
| 置信度分級 | 🔴 高 / 🟡 中 / 🟢 低，避免噪音淹沒重要問題 |
| 白名單 | `rules/whitelist.yaml`：whitelisted_patterns（rule_id+精準 pattern）+ whitelisted_paths（Windows 路徑正規化） |
| match_context | 精準錨定完整上下文行（避免誤傷其他 repo 的同模式） |
| 誤報反饋 | `--report-fp <rule-id>` 生成 GitHub issue 連結 |
| 本地白名單 | 用戶可自定義 pattern + reason |
| 置信度降級 | confidence_demotions（如 .env.example 模板文件降為 low） |

### 自掃驗證（2026-08-18 達成）

| 掃描方式 | 修復前 | 修復後 |
|---------|--------|--------|
| 遠端 GitHub | 🔴 DANGER / F / 242 發現 | 🟢 **SAFE / A / 0 發現** |
| 本地目錄 | 🔴 DANGER / F / 106 發現 | 🟢 **SAFE / A / 0 發現** |

關鍵修復：
- Windows 路徑跳過（as_posix 正規化 + 目錄組件匹配）
- 白名單覆蓋工具自身自指內容（demo/、rules/、docs/、掃描報告）
- pytest 假通過修復（return → assert，檢出率 8/8 = 100%）

---

## 10. 測試與 CI

### 測試套件（tests/test_phase0.py）

| 驗證項 | 內容 | 狀態 |
|--------|------|------|
| V-02 | YAML frontmatter 解析 | ✅ 100% |
| V-03 | Pi 版本檢測 | ✅（已入 pytest，7 用例） |
| V-04 | 正則檢測檢出率 | ✅ **8/8 = 100%** |
| V-05 | 安裝前攔截 | ⚠️ 降級為手動 URL 掃描（Pi Agent 無 hook API） |
| V-06 | 誤報基線 | ✅ 0% 誤報率 |

### Fixtures

- 惡意樣本 ×8：auto_agents_md / credential_leak / dangerous_shell / editor_git_rootkit / mcp_servers / package_persistence / sensitive_path / unicode_stego
- 乾淨樣本 ×7：hello-world / data-formatter / git-helper / code-snippet / markdown-table / mcp_safe / unicode_normal

### CI（.github/workflows/）

- `ci.yml`：多 OS × 多 Python 版本
- `update-vulns.yml`：每天 UTC 00:00 更新漏洞庫

---

## 11. 版本歷史

| 版本 | 日期 | 內容 |
|------|------|------|
| v0.1.0 | 08-17 | Phase 0 全通過 + 基礎 MVP（45 規則） |
| v1.0.0 | 08-17 | 殺手場景（URL/粘貼/三級決策） |
| v1.1.0 | 08-17 | Unicode 隱寫 + Demo 包 |
| v1.2.0 | 08-17 | 全局 AGENTS.md 檢測（critical_paths 16 條） |
| v1.3.0 | 08-17 | AI Agent 全覆蓋 + 包管理器 + 持久化（39 條） |
| v1.4.0 | 08-17 | 編輯器 + Git + 歷史 + 數據庫 + Rootkit（67 條） |
| v1.5.0 | 08-17 | Freemium + SARIF + 提示詞注入 + 擴展審計（142 條） |
| v1.6.0 | 08-17 | MCP 依賴檢查 + --all 完整掃描 + 進度顯示（158 條） |
| v2.0.0 | 08-17 | Phase 2 全部完成 + 發布推廣 |
| v3.0.0 | 08-17 | MCP 注入檢測 + 傳輸安全 + LLM 輔助檢測（181 條） |
| v3.1.0 | 08-17 | 性能優化（4x：2.4s → 0.6s）+ 完整文檔 |
| v3.2.0 | 08-17 | 每日漏洞情報系統 |
| v3.3.0 | 08-17 | 權威漏洞源 + 自動更新頻率 |
| v3.4.0 | 08-18 | installed_extensions + prompt_injection 規則（187 條 / 10 類） |
| v3.5.0 | 08-19 | 文檔重整 + 擴展審計 + Web 後端 |
| v3.6.0 | 08-24 | 隱私檢測器（pi-trail 實戰分析）+ critical_paths 精簡 |
| v3.7.0 | 08-26 | Phase 0 打包修復 + Phase 1 重構加固（cli 拆分 / 檢測器統一 / Web 安全 / pytest） |
| v3.4.x | 08-17 | 國內源（CAIVD/AVID）+ 增強狀態顯示 + 真實 4 條 CVE |
| 未發布 | 08-18 | GitHub URL 自動生成報告 / 誤報自動識別 / 掃描結論三部分 / 自掃 SAFE |

---

## 12. 安裝方式

```bash
# 1. 克隆
# 2. 依賴
pip install pyyaml
# 3. 作為 Pi Package 安裝（推薦）
pi install git:github.com/Wahero/Skill-safety-guard
# 4. 或複製/軟連結到 skills 目錄
```

### Pi Package 化（單一來源）

根目錄 `package.json` 含 `pi` manifest（`"skills": ["./SKILL.md"]`），同一份代碼即為 pi package：

- 安裝：`pi install git:github.com/Wahero/Skill-safety-guard`（或 npm 發布後 `pi install npm:skill-safety-guard`）
- 展示：含 `pi-package` keyword，可上 [pi.dev/packages](https://pi.dev/packages) 官網
- **不需維護兩份代碼**：`{baseDir}` 佔位符 + `scripts/safety-check` 自帶 sys.path，克隆即可運行

詳見 [`docs/PI_PACKAGE.md`](docs/PI_PACKAGE.md)。

---

## 13. 已知限制

| 限制 | 說明 |
|------|------|
| 安裝前自動攔截（V-05） | Pi Agent 目前無 hook API，降級為手動 URL 掃描 |
| 靜態分析邊界 | 純靜態掃描，不執行代碼；混淆/編譯型載體可能漏檢 |
| 運行時攔截 | 檢測 ≠ 防範；無實時危險命令攔截 |
| LLM 檢測環境 | 開發環境 key 脫敏，--pro 只能驗證降級路徑 |
| 免費額度 | 5 次/週，超出需等重置或升級 Pro |
| 多框架 | 目前聚焦 Pi Agent，未適配 OpenClaude/OpenCode/Claude Code |
| 版本標識 | 代碼內 `__version__ = "3.7.0"` 與功能線 v3.7.0 一致 ✅ |

---

*功能線：v3.7.0 ｜ 規則：187 條 ｜ 檢測類別：10 ｜ 數據源：OSV.dev + AVID + CAIVD*
*文檔生成：2026-08-18 ｜ 倉庫：https://github.com/Wahero/Skill-safety-guard*
