# 2026-08-26 Skill-safety-guard Phase 1 執行記錄

## 執行結果：Phase 1 全部完成 ✅

**基線**: `2aec201` (Phase 0)
**最終 Commit**: `18cda06`
**今日變更**: 16 files changed, +531 / -448（重構為主，淨增量小）

---

## 8 項完成一覽

| ID | 項目 | Commit | 說明 |
|----|------|--------|------|
| P1-1 | cli.py 拆分 | `f95a62b` | 696→417 行，拆出 scan_orchestrator.py + commands.py；web_api.py import 同步 |
| P1-2 | 統一遍歷 | `3a43617` | BaseDetector 新增 `_make_finding()` + `_detect_lines()`；6 個檢測器消除重複 |
| P1-3 | 專屬檢測器 | `3a43617` | 新增 InstalledExtensionsDetector + PromptInjectionDetector；移除 det.category hack |
| P1-4 | --version CLI | `1f7aeae` | argparse action="version" |
| P1-5 | git clone 沙箱 | `1f7aeae` | 禁用 hooks / file:// 協議 / submodule |
| P1-6 | Web 安全 | `18cda06` | CORS 收窄 / rate limit 5/min / POST 1MB 上限 / 路徑遍歷防禦 / demo banner |
| P1-7 | license DEMO 標示 | `1f7aeae` | docstring 警告 + banner 標示 + generate 時 stderr 提示 |
| P1-8 | llm_check 隱私提示 | `1f7aeae` | auth.json 使用時 stderr 提示改用環境變數 |

---

## 關鍵改動摘要

### P1-1 cli.py 拆分
- `cli.py`: 417 行（原 696 行）— 命令路由 + 參數解析 + _run_scan 編排
- `scan_orchestrator.py`: scan_target / resolve_output_file / emit_output
- `commands.py`: handle_report_fp / scan_pi_only / format_json_output / make_install_decision / format_decision_block
- `web_api.py`: import 從 cli 改為 scan_orchestrator + commands
- 修復 `license_status` 未使用變數 + `expires_at=None` 顯示 bug

### P1-2 統一遍歷
- `BaseDetector._make_finding()`: 統一構建 Finding（截斷 matched_text≤100, context≤200）
- `BaseDetector._detect_lines()`: 標準逐行正則檢測
- credentials.py / shell.py: 一行 `return self._detect_lines(...)`
- paths.py: 保留 path-env-file FP 判定，改用 _make_finding
- unicode.py: 規則匹配改用 _make_finding（保留 _format_matched_text + unicode-summary）
- critical_paths.py / privacy.py: 刪除本地 _make_finding/_make，改用 base 的

### P1-3 專屬檢測器
- 新增 `InstalledExtensionsDetector`（installed_extensions.py）
- 新增 `PromptInjectionDetector`（prompt_injection.py）
- scan_orchestrator.py 移除所有 `det.category = cat` 覆寫 hack
- 掃描迴圈從 `(det, cat)` tuple list 改為 `det` list + `det.category` 屬性

### P1-6 Web 安全加固
- CORS 從 `*` 收窄至白名單（ALLOWED_ORIGINS: localhost/127.0.0.1:8765/3000）
- 三處 CORS 響應統一：_send_json / _sse / do_OPTIONS
- Rate limiting: 每 IP 每分鐘 5 次 POST，線程安全（threading.Lock），超額 429
- POST body 大小限制 1 MB（_read_body）
- /reports/ 路徑遍歷防禦：resolve() + normpath 雙重檢查
- 啟動 banner 顯示版本號 + ⚠️ demo 模式警告

---

## 自掃白名單修復

P1-1 拆分後自掃暴露 5 個自指誤報（P0-5 加 prompt_injection 目錄掃描後首次暴露）：
- `scan_target_resolver.py`: P1-5 沙箱代碼 `core.hooksPath=NUL` 命中 critical-gitconfig-sshcommand
- `README.md` / `USAGE.md` / `PROMOTION.md` / `功能說明書.md`: 檢測能力示例引用「Ignore previous instructions」「jailbreak」命中 pi-ignore-previous / pi-jailbreak
- 使用 **path 白名單**（非 pattern），避免全局削弱規則檢測能力

---

## 驗證結果

| 測試 | 結果 |
|------|------|
| V-02 YAML 解析 | ✅ PASS |
| V-04 惡意樣本檢出率 | ✅ 9/9 = 100% |
| V-06 誤報率 | ✅ 0/7 = 0% |
| 自掃 | ✅ A / SAFE / 0 findings |
| 惡意樣本 dangerous_shell | ✅ F / DANGER / 8 critical |
| prompt_injection 目錄掃描 | ✅ 19 findings in auto_agents_md |
| Web /api/health | ✅ v3.7.0 |
| Web CORS 白名單 | ✅ 允許 localhost，拒絕 evil.com |
| Web OPTIONS preflight | ✅ 204 |
| Web rate limit | ✅ 5×202 → 429 |
| Web path traversal | ✅ 404 |

---

## 執行中遇到的坑

1. **P1-1 拆分後自掃 F/5**：5 個自指誤報。用 path 白名單（非 pattern）修復，避免全局削弱規則。
2. **privacy.py `return findings` 縮排錯誤**：edit_file 把 `return findings` 放到 `for match` 迴圈內部（24 空格縮排），無匹配時隱式回傳 None → `TypeError: 'NoneType' object is not iterable`。修復後 8 個檢測器全部正常。
3. **PowerShell 引號轉義**：`--output json` + `Select-Object` 在 PowerShell 每次報錯。改用獨立 Python 驗證腳本（`_verify_p1*.py`）+ `--output-file` 寫臨時檔解析，徹底繞開。

---

## 已提交 Commit

```
1f7aeae Phase 1: 4 項獨立快速修復
f95a62b Phase 1: P1-1 拆分 cli.py + 自掃白名單修復
3a43617 Phase 1: P1-2 統一遍歷 + P1-3 專屬檢測器
18cda06 Phase 1: P1-6 Web 安全加固
```

---

## 下一步

進入 **Phase 2：測試與文檔同步**（4 項）：
- P2-1: pytest 補 V-03 / V-05
- P2-2: 誤報反饋文檔
- P2-3: 白名單貢獻教程
- P2-4: CHANGELOG / README / SKILL.md 同步
